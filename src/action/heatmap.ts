/**
 *  热力图模块
 *
 * @file heatMap.ts
 * @author guyunlong
 */

import { HeatmapConfig } from "../../types/action";

class Heatmap {
  public install(config: HeatmapConfig) {}
}

const heatmap = new Heatmap();
export default heatmap;
