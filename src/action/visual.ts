/**
 *  可视化埋点模块
 *
 * @file visual.ts
 * @author guyunlong
 */

import { VisualConfig } from "../../types/action";

class Visual {
  public install(config: VisualConfig) {}
}

const visual = new Visual();
export default visual;
